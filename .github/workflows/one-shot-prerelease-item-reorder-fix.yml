name: One-shot prerelease item reorder fix

on:
  push:
    branches: [prerelease]
    paths:
      - '.github/workflows/one-shot-prerelease-item-reorder-fix.yml'

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: prerelease
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Patch and validate prerelease
        run: |
          set -euo pipefail
          cp .github/scripts/prerelease-item-reorder-fix.py /tmp/prerelease-item-reorder-fix.py
          python3 /tmp/prerelease-item-reorder-fix.py
          npm ci
          npm run check
          git config user.name "Lunarch"
          git config user.email "phantanlocct@outlook.com"
          git add src/icon-library-setting-tab.ts styles.css tests/multi-dock.test.ts CHANGELOG.md main.js
          git commit -m "Fix Dock item reorder controls in settings"
          git push origin prerelease
      - name: Remove temporary reorder tooling
        if: always()
        run: |
          set -euo pipefail
          git fetch origin prerelease
          git reset --hard origin/prerelease
          git config user.name "Lunarch"
          git config user.email "phantanlocct@outlook.com"
          git rm -f .github/workflows/one-shot-prerelease-item-reorder-fix.yml .github/scripts/prerelease-item-reorder-fix.py
          git commit -m "Remove temporary prerelease reorder tooling"
          git push origin prerelease
